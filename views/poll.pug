extends layout

include mixins/_listModal

block styles
  link(rel="stylesheet" href="poll.css")

block content
  .section#poll
    .is-narrow.div.mcenter
      h1 Poll - #{user}
        ul
          each anime in poll.list
            li
              .columns.is-mobile
                .column.is-10 #{anime.title}
                .column.is-2 #{anime.votes}
              +listModal(anime)