mixin listModal(anime = {})
  .modal(id=`modal-${anime.id}`)
    .modal-background
    .modal-content
      .box.modal-box
        p.tcenter
          a(href=`https://myanimelist.net/anime/${anime.id}/` rel='noopener noreferrer' target='_blank' title=anime.title) #{anime.title}
        figure.tcenter
          img(src=anime.image)
        p.tcenter
          button.button.is-primary.vote-button Vote!
    button.modal-close